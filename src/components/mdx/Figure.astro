---
interface Props {
  src?: string;
  alt?: string;
  caption?: string;
  id?: string;
  number?: number;
}
const { src, alt, caption, id, number } = Astro.props;
---

<figure class="paper-figure" id={id}>
  <div class="figure-stage">
    {src && <img class="figure-image" src={src} alt={alt ?? ''} loading="lazy" decoding="async" />}
    <slot />
  </div>
  {caption && (
    <figcaption>
      <strong>Figure{number !== undefined && ` ${number}`}</strong>
      <span>{caption}</span>
    </figcaption>
  )}
</figure>

<style>
  .paper-figure {
    width: 100%;
    min-width: 0;
    margin: 2rem 0;
  }

  .figure-stage {
    display: grid;
    width: 100%;
    min-width: 0;
    box-sizing: border-box;
    place-items: center;
    overflow-x: auto;
    overscroll-behavior-inline: contain;
    border: 1px solid color-mix(in oklab, var(--data-structure, var(--accent)) 15%, var(--rule));
    border-radius: 0.85rem;
    background:
      radial-gradient(circle at 50% 0%, color-mix(in oklab, var(--data-structure, var(--accent)) 7%, transparent), transparent 54%),
      var(--surface, color-mix(in oklab, var(--bg) 97%, var(--fg) 3%));
    box-shadow: 0 0.9rem 2.8rem color-mix(in oklab, var(--fg) 5%, transparent);
    -webkit-overflow-scrolling: touch;
    scrollbar-width: thin;
  }

  .figure-image {
    display: block;
    width: auto;
    max-width: 100%;
    height: auto;
    margin-inline: auto;
    object-fit: contain;
    border-radius: calc(0.85rem - 1px);
  }

  .paper-figure figcaption {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 0.55rem;
    margin-top: 0.75rem;
    padding: 0 0.25rem;
    color: var(--fg-muted);
    font-family: var(--font-sans);
    font-size: 0.78rem;
    line-height: 1.55;
    text-align: left;
  }

  .paper-figure figcaption strong {
    align-self: start;
    padding: 0.17rem 0.42rem;
    border-radius: 999px;
    background: color-mix(in oklab, var(--data-structure, var(--accent)) 11%, transparent);
    color: var(--data-structure, var(--accent));
    font-size: 0.65rem;
    font-weight: 750;
    letter-spacing: 0.04em;
    line-height: 1.35;
    text-transform: uppercase;
  }

  :global(.figure-stage > *) { max-width: 100%; }
  :global(.figure-stage img) { max-width: 100%; height: auto; }
  :global(.figure-stage svg),
  :global(.figure-stage canvas),
  :global(.figure-stage video) { max-width: 100%; }

  @media (max-width: 640px) {
    .paper-figure { margin-block: 1.6rem; }
    .paper-figure figcaption { grid-template-columns: 1fr; gap: 0.4rem; }
    .paper-figure figcaption strong { justify-self: start; }
  }
</style>
